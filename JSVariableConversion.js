// Declare a number and a string variable
var numberVariable = 10;
var stringVariable = "20";

// Convert the string into a number
var convertedNumber = parseInt(stringVariable);

// Sum the two numbers
var sum = numberVariable + convertedNumber;

// Log the result
console.log("Sum:", sum);
